{
  "routes": ["GET /environments"],
  "response": {
    "environments": [
      {
        "name": "zsh",
        "kind": "shell",
        "label": "Zsh",
        "description": "Run inside a login shell",
        "default": true
      },
      {
        "name": "direct",
        "kind": "direct"
      }
    ]
  }
}
